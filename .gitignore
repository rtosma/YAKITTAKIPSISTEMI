# Dependencies
node_modules/
backend/node_modules/
frontend/node_modules/

# Build outputs
build/
dist/
backend/dist/
frontend/dist/

# Coverage
coverage/

# OS files
.DS_Store

# Logs
*.log

# Environment files (except examples)
.env*
!.env.example
backend/.env*
!backend/.env.example
frontend/.env*
!frontend/.env.example

# npm cache
.npm-cache/

# Docker volumes (if local)
pgdata/

# Claude Code local (personal) settings — not shared with the team
.claude/settings.local.json

# Playwright E2E çıktıları (ekran görüntüsü/trace — commit edilmez)
frontend/test-results/
frontend/playwright-report/

# NFS geçici dosyaları (silinmiş ama hâlâ açık olan dosyaların kalıntısı —
# bu proje NFS üzerinde barındığı için oluşuyor, versiyonlanmamalı)
.nfs*
**/.nfs*
